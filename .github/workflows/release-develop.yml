name: Release Develop

on:
  workflow_run:
    workflows: [Config]
    types: [completed]
    branches: [develop]

jobs:
  build:
    uses: WatWowMap/ReactMap/.github/workflows/release-base.yml@develop
